nixPkgs = ["python311", "python311Packages.pip", "nodejs_20", "chromium", "mesa-libgbm", "libxshmfence"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npx playwright install --with-deps chromium"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port=7777"
