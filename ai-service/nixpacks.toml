[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20", "chromium", "libgbm", "libxshmfence"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npx playwright install chromium"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port=7777"
