[phases.setup]
nixPkgs = ["python3", "nodejs_20", "chromium"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt", "playwright install --with-deps chromium"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port=4000"
