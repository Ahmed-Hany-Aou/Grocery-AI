[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "nodejs_20", "chromium"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps chromium"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port=7777"
